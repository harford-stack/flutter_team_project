import 'package:flutter/material.dart';

/// 앱에서 사용하는 색상 상수
class AppColors {
  AppColors._(); // 인스턴스 생성 방지

  // 배경색
  static const Color backgroundColor = Color(0xFFFAFAF8);

  // 포인트 컬러
  static const Color primaryColor = Color(0xFF035AA6);

  // 서브 컬러
  static const Color secondaryColor = Color(0xFF6DA7F2);

  // 텍스트 색상
  static const Color textWhite = Colors.white; // 기본 흰색
  static const Color textDark = Color(0xFF1A1A1A); // 기본 다크 텍스트
}

