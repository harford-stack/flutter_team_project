import 'package:flutter/material.dart';

class RecipeShakeScreen extends StatelessWidget {
  const RecipeShakeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('레시피 흔들기 화면'),
      ),
    );
  }
}
