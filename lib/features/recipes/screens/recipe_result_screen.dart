import 'package:flutter/material.dart';

class RecipeResultScreen extends StatelessWidget {
  const RecipeResultScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('레시피 결과 화면'),
      ),
    );
  }
}
