## 프로젝트 구조

### 📁 최소 구조

```
lib/
├── common/              # 공통 사용 파일 (공통 위젯, 유틸리티 함수, 상수 등)
├── main.dart            # 앱 시작점 (필수)
└── firebase_options.dart # Firebase 설정 (자동 생성됨)
```

### 📁 작업 구조 (각 팀원이 자유롭게 생성)

각 팀원은 본인이 맡은 기능에 대해 **본인이 폴더를 만들어서** 작업합니다.

**예시:**
```
lib/
├── main.dart
├── firebase_options.dart
│
├── auth/                 # 팀원A: 인증 기능 담당
│   ├── login_screen.dart
│   ├── signup_screen.dart
│   └── auth_service.dart
│
├── ingredients/          # 팀원B: 재료 관리 기능 담당
│   ├── ingredient_list_screen.dart
│   ├── ingredient_add_screen.dart
│   ├── ingredient_model.dart
│   └── ingredient_service.dart
│
├── recipes/              # 팀원C: 레시피 기능 담당
│   ├── recipe_list_screen.dart
│   ├── recipe_detail_screen.dart
│   ├── recipe_model.dart
│   └── recipe_service.dart
│
└── community/            # 팀원D: 커뮤니티 기능 담당
    ├── community_list_screen.dart
    ├── community_detail_screen.dart
    ├── post_model.dart
    └── community_service.dart
```

---

## 작업 방식

### ✅ 권장 사항

1. **본인 폴더에서 작업**
   - 각 팀원은 본인이 맡은 기능에 대한 폴더를 `lib/` 아래에 생성
   - 폴더명은 기능명으로 명확하게 작성 (예: `auth/`, `ingredients/`, `recipes/`)

2. **폴더 내 구조는 자유롭게**
   - 각 팀원이 본인 폴더 안에서 자유롭게 구조 결정
   - 예: `screens/`, `widgets/`, `models/`, `services/` 등 필요에 따라 생성

3. **파일 네이밍 규칙**
   - 화면: `[기능명]_screen.dart` (예: `login_screen.dart`)
   - 위젯: `[위젯명]_widget.dart` (예: `loading_widget.dart`)
   - 모델: `[모델명]_model.dart` 또는 `[모델명].dart` (예: `ingredient_model.dart`)
   - 서비스: `[서비스명]_service.dart` (예: `auth_service.dart`)

4. **공통 사용 파일**
   - 여러 기능에서 공통으로 사용하는 파일은 `lib/common/` 폴더에 생성
   - 예: 공통 위젯, 유틸리티 함수, 상수 등

### ⚠️ 주의 사항

1. **다른 팀원의 폴더는 수정하지 않기**
   - 각 팀원의 폴더는 해당 팀원이 담당
   - 필요시 팀 회의를 통해 협의 후 수정

2. **충돌 방지**
   - Git 작업 전 `git pull`로 최신 상태 확인
   - 같은 파일을 수정할 경우 팀 회의를 통해 협의

3. **코드 리뷰**
   - Pull Request를 통해 코드 리뷰 진행
   - 팀원 간 피드백 공유

---

## 시작하기

### 1. 프로젝트 클론 후 설정

```bash
# 저장소 클론
git clone [저장소 URL]
cd flutter_team_project

# 패키지 설치
flutter pub get

# .env 파일 생성 (프로젝트 루트에)
# GEMINI_API_KEY=your_api_key_here
```

### 2. .env 파일 설정

프로젝트 루트에 `.env` 파일을 생성하고 다음 내용을 추가하세요:

```
GEMINI_API_KEY=your_gemini_api_key_here
```

⚠️ **주의**: `.env` 파일은 Git에 커밋되지 않습니다. 각자 본인의 API 키를 발급받아 사용하세요.

### 3. Firebase 설정

- Firebase 프로젝트 생성
- `flutterfire configure --project=flutterteamproject-ae948` 실행
- **SHA-1 키 등록** (구글 로그인용)
  - ⚠️ **각 팀원마다 SHA-1 키가 다릅니다!**
  - 각자 본인의 SHA-1 키를 Firebase Console에 등록해야 합니다
  - 자세한 방법은 `SETUP_GUIDE.md` 참고

### 4. 작업 시작

1. **본인이 맡은 기능 확인**
2. **`lib/` 아래에 본인 폴더 생성** (예: `lib/auth/`)
3. **필요한 파일 생성 및 작업 시작**

---

## 📝 작업 예시

### 예시 1: 인증 기능 담당 팀원

```bash
# lib/auth/ 폴더 생성
mkdir lib/auth

# 필요한 파일 생성
# - login_screen.dart
# - signup_screen.dart
# - auth_service.dart
```

### 예시 2: 재료 관리 기능 담당 팀원

```bash
# lib/ingredients/ 폴더 생성
mkdir lib/ingredients

# 필요한 파일 생성
# - ingredient_list_screen.dart
# - ingredient_add_screen.dart
# - ingredient_model.dart
# - ingredient_service.dart
```

---

## ✅ 체크리스트

코드를 작성할 때:

- [ ] 본인 폴더를 `lib/` 아래에 생성했나요?
- [ ] 파일 네이밍 규칙을 따랐나요?
- [ ] 다른 팀원의 폴더를 수정하지 않았나요?
- [ ] 공통 사용 파일은 `lib/common/` 또는 `lib/shared/`에 두었나요?
- [ ] Git 작업 전 `git pull`을 했나요?

---

## ❓ 자주 묻는 질문

**Q: 새로운 기능, 화면면을 추가하려면?**  
A: `lib/` 아래에 새 폴더를 만들고 작업하세요.

**Q: 공통으로 사용하는 위젯은 어디에?**  
A: `lib/common/` 폴더에 생성하세요.

**Q: 다른 팀원의 코드를 사용하고 싶어요**  
A: 해당 파일을 import하여 사용할 수 있습니다. 예: `import 'package:flutter_team_project/auth/auth_service.dart';`

**Q: 폴더 구조가 복잡해졌어요**  
A: 각 팀원이 본인 폴더 안에서 구조를 정리하되, 팀 회의를 통해 일관성 있게 유지하세요.

---

## 📚 추가 가이드

- **프로젝트 설정**: `SETUP_GUIDE.md` 참고
- **체크리스트**: `CHECKLIST.md` 참고
- **SHA-1 키 설명**: `SHA1_EXPLANATION.md` 참고

---

이 가이드를 따라주시면 팀원 간 충돌 없이 효율적으로 작업할 수 있습니다! 🚀
